version: '3'

services:
  scrapy:
    build: .
    image: scrapy
    restart: always
    network_mode: host
    container_name: scrapy
    environment:
      TZ: Asia/Taipei
      GMAIL_USER: a0970366492@gmail.com
      LOG_DIR: /data/log
      GMAIL_PASSWORD: 
    ports:
      - '5000:5000'
    volumes:
      - /data/log:/data/log